<x-layouts.app :title="'Registrations · '.$event->name" wide :breadcrumbs="[['label'=>'Events','url'=>route('events.index')],['label'=>$event->name,'url'=>route('events.show',$event)],['label'=>'Registrations']]">
    <x-ui.page-header title="Registrations" subtitle="Public and manager-entered submissions are independent of Ticketing."><x-slot:actions><a class="btn btn-outline-primary" href="{{ route('events.registration-forms.index',$event) }}">Form builder</a>@can('export',[App\Models\Registration::class,$event])<a class="btn btn-outline-secondary" href="{{ route('events.registrations.export',[$event]+request()->query()) }}">Export CSV</a>@endcan</x-slot:actions></x-ui.page-header>
    <form class="card card-body mb-4" method="GET"><div class="row g-3 align-items-end"><div class="col-md-4"><label class="form-label" for="q">Search</label><input class="form-control" id="q" name="q" maxlength="100" value="{{ $filters['q'] ?? '' }}"></div><div class="col-md-2"><label class="form-label" for="status">Status</label><select class="form-select" id="status" name="status"><option value="">All</option>@foreach(App\Models\Registration::STATUSES as $status)<option value="{{ $status }}" @selected(($filters['status']??null)===$status)>{{ str($status)->headline() }}</option>@endforeach</select></div><div class="col-md-2"><label class="form-label" for="source">Source</label><select class="form-select" id="source" name="source"><option value="">All</option><option value="public" @selected(($filters['source']??null)==='public')>Public</option><option value="offline" @selected(($filters['source']??null)==='offline')>Offline</option></select></div><div class="col-md-3"><label class="form-label" for="form_id">Form</label><select class="form-select" id="form_id" name="form_id"><option value="">All</option>@foreach($forms as $form)<option value="{{ $form->id }}" @selected((string)($filters['form_id']??'')===(string)$form->id)>{{ $form->name }}</option>@endforeach</select></div><div class="col-auto"><button class="btn btn-primary" type="submit">Filter</button></div></div></form>
    <x-ui.data-table :columns="[['label'=>'Reference'],['label'=>'Registrant'],['label'=>'Form'],['label'=>'Source'],['label'=>'Status'],['label'=>'Submitted']]" caption="Registration submissions" :empty="$registrations->isEmpty()" empty-title="No registrations found">
        @foreach($registrations as $registration)<tr><td><a href="{{ route('events.registrations.show',[$event,$registration]) }}">{{ $registration->reference_number }}</a></td><td><strong>{{ $registration->registrant_name }}</strong><span class="d-block small text-secondary">{{ $registration->registrant_email ?: $registration->registrant_phone }}</span></td><td>{{ $registration->form->name }}</td><td>{{ str($registration->source)->headline() }}</td><td><x-ui.status-badge :status="$registration->status"/></td><td>{{ $registration->submitted_at->format('Y-m-d H:i') }}</td></tr>@endforeach
    </x-ui.data-table><div class="mt-3">{{ $registrations->links() }}</div>
</x-layouts.app>
