<x-layouts.app :title="$category->name" :breadcrumbs="[['label' => 'Overview', 'url' => route('home')], ['label' => $definition['label'], 'url' => route('settings.master-data.index', $type)], ['label' => $category->name]]">
    <x-ui.page-header :title="$category->name" :subtitle="'Update this '.Str::lower($definition['singular']).'.'"/>
    @include('settings.master-data._tabs')
    <form class="card mb-4" method="POST" action="{{ route('settings.master-data.update', [$type, $category->id]) }}">@csrf @method('PUT')<div class="card-body">@include('settings.master-data._form')</div><div class="card-footer bg-white d-flex justify-content-end gap-2"><a class="btn btn-outline-secondary" href="{{ route('settings.master-data.index', $type) }}">Back</a><button class="btn btn-primary" type="submit">Save category</button></div></form>
    @can('delete', $category)<form id="archive-category-form" method="POST" action="{{ route('settings.master-data.destroy', [$type, $category->id]) }}">@csrf @method('DELETE')<button class="btn btn-outline-danger" type="button" data-bs-toggle="modal" data-bs-target="#confirmationModal" data-confirm-form="archive-category-form" data-confirm-title="Archive this category?" data-confirm-message="Existing historical references will remain valid, but the category will not be offered for new records." data-confirm-button="Archive category">Archive category</button></form>@endcan
</x-layouts.app>
