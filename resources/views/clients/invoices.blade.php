<x-layouts.app title="Client Invoices" :breadcrumbs="[['label' => 'Clients', 'url' => route('clients.index')], ['label' => $client->display_name, 'url' => route('clients.show', $client)], ['label' => 'Invoices']]" wide>
    <x-ui.page-header title="Client Invoice History" :subtitle="$client->display_name"/>
    <form class="card card-body mb-4" method="GET"><div class="row g-3"><div class="col-md-6"><label class="form-label" for="q">Search</label><input class="form-control" id="q" name="q" value="{{ $filters['q'] ?? '' }}"></div><div class="col-md-3"><label class="form-label" for="status">Status</label><select class="form-select" id="status" name="status"><option value="">All</option>@foreach([...\App\Models\Invoice::STATUSES, 'overdue'] as $status)<option value="{{ $status }}" @selected(($filters['status'] ?? '') === $status)>{{ str($status)->headline() }}</option>@endforeach</select></div><div class="col-md-2 align-self-end"><button class="btn btn-outline-primary">Filter</button></div></div></form>
    <div class="card"><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Invoice</th><th>Event</th><th>Status</th><th class="text-end">Total</th><th class="text-end">Balance</th></tr></thead><tbody>@forelse($invoices as $invoice)<tr><td><a href="{{ route('events.invoices.show', [$invoice->event_id, $invoice]) }}">{{ $invoice->invoice_number ?? 'Draft #'.$invoice->id }}</a></td><td>{{ $invoice->event->name }}</td><td><x-ui.status-badge :status="$invoice->displayStatus()"/></td><td class="text-end">{{ $invoice->currency_code }} {{ number_format((float) $invoice->total, 2) }}</td><td class="text-end">{{ $invoice->currency_code }} {{ number_format((float) $balances->balance($invoice), 2) }}</td></tr>@empty<tr><td colspan="5"><x-ui.empty-state title="No Invoices" message="No enabled Event has an Invoice for this Client."/></td></tr>@endforelse</tbody></table></div></div><x-ui.pagination :paginator="$invoices"/>
</x-layouts.app>
