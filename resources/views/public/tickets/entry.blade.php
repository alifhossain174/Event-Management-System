<x-layouts.app :title="$ticket->ticket_number"><div class="container py-5"><article class="card card-body mx-auto text-center" style="max-width:40rem"><h1 class="h3">{{ $ticket->event->name }}</h1><h2 class="h5">{{ $ticket->type->name }}</h2><p>{{ $ticket->attendee_name }}</p><p><strong>{{ $ticket->ticket_number }}</strong></p>@if($ticket->status==='issued')<img class="img-fluid d-block mx-auto" width="280" height="280" alt="Admission QR" src="{{ route('public.tickets.qr',['token'=>request()->route('token')]) }}">@endif<div class="alert {{ $ticket->status==='issued'?'alert-success':'alert-warning' }}">{{ $ticket->status==='issued'?'Valid for one admission':str($ticket->status)->headline().' — not valid for admission' }}</div><p class="small text-secondary">Present this private link at check-in. Do not share it.</p></article></div></x-layouts.app>
