# Requirements Traceability

## Purpose and baseline

This matrix maps the full scope in `Event_Management_System_SRS_Laravel_MySQL_v1.2.docx` to an implementation phase and planned verification. It covers all 29 functional modules, FR-X01 through FR-X13, and NFR-01 through NFR-13. The source SRS remains authoritative for detailed behavior; this file controls implementation sequencing and test coverage.

No row is considered implemented merely because a migration, screen, or class exists. Completion requires the listed automated tests and any stated manual verification to pass.

## Planned phases

| Phase | Scope |
| --- | --- |
| P0 Environment and project bootstrap | Laravel 12 scaffold, Git, local database and application configuration, test harness, and build verification. |
| P1 Foundation and core Client to Event flow | Authentication, RBAC, settings, master records, Event categories/templates, per-Event modules, direct Event creation, basic dashboard/search, protected documents, and audit. |
| P2 Common operations and finance | Optional Booking, Venue, vendor/staff assignments, Tasks, Budget, manual Payments, Invoices, Calendar, static Notifications, and Communication Center. |
| P3 Optional event services | Guest, Registration, Ticketing, Inventory, Catering, Decoration, Transportation, Accommodation, and Marketing. |
| P4 Reporting and business completion | Reports, Analytics, reconciliation, performance reporting, and module/template usability refinement. |
| P5 Hardening and deployment | Security, privacy, accessibility, performance, browser coverage, backup/restore, cPanel deployment, and user acceptance testing. |

## Implemented evidence

Prompt 04 provides partial P1 evidence for M02, M29, FR-X02, FR-X03, FR-X04, NFR-01, NFR-04, and NFR-07:

- `AuthenticationTest` covers active login/logout, inactive rejection, session termination, login history, and disabled public registration.
- `PasswordResetTest` covers active-user reset and suppresses reset delivery for inactive users.
- `ProfileTest` covers authenticated profile/password changes and their audit records.
- `UserManagementTest` covers seven-role seeding, role assignment, policy denial on direct routes, full Administrator / Business Manager permissions, status history, audit hooks, filters, and pagination.

This is not completion evidence for M29 or the cross-cutting requirements beyond identity/security; later domain prompts must extend the audit and authorization matrix for their own sensitive actions.

Prompt 05 adds partial P1 evidence for FR-X02, NFR-01, NFR-04, NFR-08, NFR-09, and NFR-13 through AdministrationShellTest: responsive administration landmarks, permission-filtered navigation with independent direct-route denial, semantic feedback/error patterns, escaped component output, server-side pagination presentation, and a local/testing-only component reference. Full keyboard/browser/device auditing remains planned for P5.

Prompt 06 adds P1 evidence for M28, FR-X02, FR-X03, FR-X06, FR-X07, FR-X08, NFR-01, NFR-04, NFR-07, NFR-10, NFR-12, and NFR-13 through `SettingsManagementTest` and `MasterDataManagementTest`: typed cached settings, encrypted/masked secrets, company branding metadata, disabled-by-default branches, explicit null-safe branch scope, direct-route authorization denial, separate configurable category domains, filters, pagination, soft archive, and redacted audit entries. Backup, provider, Event, and production branch-isolation completion remain assigned to their later phases.

Prompt 08 adds P1 evidence for M04, FR-X02, FR-X03, FR-X04, FR-X05, FR-X07, FR-X11, NFR-01, NFR-02, NFR-04, NFR-07, and NFR-12 through `ClientManagementTest`: individual/organization creation without a User, normalized search, advisory duplicates that allow shared contacts, pagination/filters, explicit branch scope, contact management, archive/reactivate and merge history, no destructive Client route, policy denial, privileged portal linking, protected-document context, and Event-form lookup/quick-create using shared validation. Event links, the Draft transition gate, and financial/communication summaries remain assigned to their owning prompts.

Prompt 10 adds P1 evidence for M03, FR-X02, FR-X03, FR-X04, FR-X08, FR-X10, FR-X12, FR-X13, NFR-01, NFR-04, NFR-07, NFR-12, and NFR-13 through `EventTemplateManagementTest`: 29 stable definitions, 18 event-scoped keys, exact editable SRS templates, paginated/filterable protected administration, audit/status history, immutable plan snapshots, safe rejection of unknown/global keys, warning-only dependencies, and a blank Custom plan with all specialized modules disabled. Persisted per-Event configuration and disable/re-enable data preservation remain assigned to Prompt 11.

Prompt 11 adds P1 evidence for M03, M04, M05, FR-X02, FR-X03, FR-X04, FR-X06, FR-X07, FR-X09, FR-X10, FR-X12, FR-X13, NFR-01, NFR-04, NFR-07, NFR-10, NFR-12, and NFR-13 through `EventLifecycleTest`: direct Event creation with null booking_id, nullable Client only in Draft, every forward transition, cancellation traceability, completed read-only behavior and privileged correction, independent duplication exclusions, UTC/time-zone conversion, persisted editable module states, all-disabled completion, authorization, filters, and pagination.

Prompt 12 adds P1 evidence for M03, M24, FR-X04, FR-X10, FR-X12, FR-X13, NFR-01, NFR-04, NFR-07, NFR-08, NFR-12, and NFR-13 through `EventWorkspaceTest`: enabled-and-authorized workspace projection, manager-only module administration, safe disabled/unknown direct routes, template/manual origin indicators, bounded data detection, explicit populated-disable confirmation/reason, append-only actor/time history, record preservation, re-enable restoration, and paginated/filterable history. `EventLifecycleTest` remains the all-modules-disabled completion proof.

Prompt 13 adds P1 evidence for M01, FR-X01, FR-X02, FR-X03, FR-X06, NFR-01, NFR-03, NFR-04, NFR-07, NFR-08, and NFR-12 through `DashboardAndSearchTest`: live Event KPI/list reconciliation, date/branch/category/manager filter propagation, branch-isolated counts and results, permission-checked provider search for implemented Events/Clients/Vendors/Users, own-record Vendor search, escaped output, index-friendly bounded queries, authorized empty states, and a common-path query budget. Booking/Invoice search providers, task/finance widgets, production-volume performance, and full browser timing remain assigned to their owning phases.

Prompt 14 adds P2 evidence for M01, M05, M24, FR-X01, FR-X02, FR-X03, FR-X04, FR-X09, FR-X12, NFR-01, NFR-04, NFR-07, NFR-10, NFR-12, and NFR-13 through `OptionalBookingWorkflowTest`: manager-created enquiry, review, waitlist/promotion, one-step approval/confirmation, complete actor/time/reason status history, conflict-aware rescheduling with privileged explicit override, cancellation finance flags without deletion, protected document ownership, BranchScope, bounded dashboard/search projection, editable conversion category/template/modules, transactional nullable bidirectional links, repeat-conversion idempotence, and direct Event completion with no Booking.

Prompt 15 adds P2 evidence for M06, M24, FR-X02, FR-X03, FR-X04, FR-X07, FR-X08, FR-X12, FR-X13, NFR-01, NFR-04, NFR-07, NFR-10, NFR-12, and NFR-13 through `VenueManagementTest`: owned/third-party Venue creation, branch-aware authorization, room-specific availability, adjacent versus overlapping interval rules, cancelled-Event release, expected-guest capacity warnings, transactional parent locking, privileged reasoned override audit, allocation history, detector-backed disabled access, preservation, and re-enable. Existing Document tests cover the protected image storage/download boundary.

Prompt 16 adds P2 evidence for M07, M24, FR-X02, FR-X03, FR-X04, FR-X07, FR-X11, FR-X12, FR-X13, NFR-01, NFR-04, NFR-07, NFR-10, NFR-12, and NFR-13 through VendorAssignmentTest: warn/block availability rules, half-open overlap checks under a locked Vendor parent, manager-owned assignments/work orders/contracts/cost approval, linked-Vendor ownership isolation and delivery updates, archived-master history, completed-work-only ratings, protected invoice files, detector-backed disabled access/preservation, and a read-only approved-cost finance contract that creates no Expense or Payment. Actual Finance posting remains assigned to its owning prompts.

Prompt 17 adds P2 evidence for M08, FR-X02, FR-X03, FR-X04, FR-X07, FR-X11, FR-X12, FR-X13, NFR-01, NFR-02, NFR-04, NFR-07, NFR-10, NFR-12, and NFR-13 through StaffOperationsTest: manager-on-behalf scheduling for profiles without Users, locked half-open overlap checks, approved-leave availability, separately authorized reasoned overrides, linked-Staff ownership isolation and work updates, reportable attendance inputs, salary confidentiality and non-payroll scope, archive integrity, and detector-backed disabled access/preservation.

Prompt 18 adds P2 evidence for M12, M23, M24, FR-X02, FR-X03, FR-X04, FR-X05, FR-X06, FR-X07, FR-X12, FR-X13, NFR-01, NFR-04, NFR-07, NFR-10, NFR-12, and NFR-13 through `TaskWorkflowTest` and `CalendarProjectionTest`: manager/User/Staff assignment, linked-Staff own visibility, completion actor/time, derived overdue, protected Task attachments, filters/pagination, dedicated append-only status history, module guard/data preservation, daily/weekly/monthly UTC windows, authorized click-through, disabled-source omission, and proof that no duplicate Calendar source table exists.

Prompt 19 adds P2 evidence for M13, M24, FR-X02, FR-X03, FR-X04, FR-X05, FR-X06, FR-X08, FR-X12, FR-X13, NFR-01, NFR-04, NFR-07, NFR-10, NFR-12, and NFR-13 through `EventBudgetTest` and `DecimalMathTest`: separate planned/actual records, SRS FinanceCategory defaults, exact four-decimal arithmetic, category variance, posted-only recognition/profit, immutable posted entries, linked reversals, permission separation, Event isolation, protected evidence ownership, idempotent source identities, and detector-backed disable/preserve/re-enable behavior.

Prompt 20 adds P2 evidence for M04, M13, M14, FR-X02, FR-X03, FR-X04, FR-X06, FR-X09, FR-X12, FR-X13, NFR-01, NFR-02, NFR-04, NFR-07, NFR-10, NFR-11, NFR-12, and NFR-13 through `ManualPaymentLedgerTest`: separate installment entries, exact allocation/due math, unallocated advances, positive-only amounts, unique receipts, idempotent submissions, locked due rechecks, allocated/unallocated refund limits, due reopening, linked Income/reversal recognition, immutable originals, audit/status history, permission-protected Event/Client/due/receipt views, disabled-module preservation, invoice-ready calculation, and proof that no gateway/checkout/webhook route exists.

Prompt 21 adds P2 evidence for M13, M14, M15, FR-X01, FR-X02, FR-X03, FR-X04, FR-X06, FR-X09, FR-X12, FR-X13, NFR-01, NFR-02, NFR-04, NFR-07, NFR-10, NFR-11, NFR-12, and NFR-13 through `InvoiceWorkflowTest`, `ManualPaymentLedgerTest`, and `DecimalMathTest`: nullable Booking-independent Invoice creation, locked prefix/year sequencing, deterministic discount-before-tax rounding, immutable party/item/tax snapshots, derived Overdue, cancellation/credit evidence, partial/full/refunded allocation reconciliation, explicit unallocated overpayment, permission-aware search and Event/Client history, local PDF/print totals, synchronous email success/failure logging, restrictive foreign keys, authorization, and optional-module preservation.

Prompt 22 adds P2 evidence for M05, M07, M08, M14, M22, M27, M29, FR-X02, FR-X03, FR-X07, FR-X12, FR-X13, NFR-01, NFR-02, NFR-04, NFR-07, NFR-10, NFR-11, NFR-12, and NFR-13 through `CommunicationCenterTest`: recipient-isolated read/unread state, escaped content, allowlisted route links, user/role targeting, synchronous email completion, separately confirmed marketing consent, disabled-provider failure and retry history, idempotent Event reminders, permission denial, Event-module guarding, and preservation/re-enable. Existing Booking, Payment, Staff, and Vendor tests exercise the newly connected notification hooks. Low-stock notification creation remains assigned to Inventory because that source does not yet exist.

Prompt 23 adds P3 evidence for M06, M09, M29, FR-X02, FR-X03, FR-X04, FR-X06, FR-X07, FR-X12, FR-X13, NFR-01, NFR-02, NFR-04, NFR-07, NFR-10, NFR-12, and NFR-13 through `GuestManagementTest` and `GuestCheckInTest`: no-login Guest records, import-ready/search-normalized fields, groups/VIP/RSVP/plus-one rules, unique Event seating, private-note isolation, Venue capacity warnings, non-guessable token uniqueness, QR privacy, Event isolation, revoked/invalid handling, idempotent double scan with original actor/time, Front Desk permission boundaries, audit/status history, and detector-backed disabled access/preservation.

Prompt 25 adds P3 evidence for M10, M14, FR-X02, FR-X03, FR-X04, FR-X06, FR-X12, FR-X13, NFR-01, NFR-02, NFR-04, NFR-07, NFR-10, NFR-11, NFR-12, and NFR-13 through `TicketingManagementTest`: transactional general-admission capacity, serialized last-Ticket issuance, deterministic price/promo snapshots and ticket-count usage limits, opaque token secrecy, Event-isolated one-time scan history, refund/cancellation invalidation, manual Payment/Income-reversal trace, granular Manager/Finance/Front Desk permissions, explicit public publication, detector-backed preservation, and Registration operation with Ticketing disabled.

## Functional module traceability

| ID | SRS module | Planned phase | Planned test evidence |
| --- | --- | --- | --- |
| M01 | Dashboard | P1 baseline implemented; P2/P5 completion | `DashboardAndSearchTest`: current Event status counts, authorized filters, branch isolation, empty states, linked drill-down reconciliation, and bounded queries. Later tests add task/finance widgets only after their source modules exist, then production-volume/browser timing. |
| M02 | User Management | P1; P5 security hardening | `UserAdministrationTest`: create, deactivate, search, reset password, and reject disabled login. `AuthorizationMatrixTest`: protected actions are denied server-side for every role. |
| M03 | Event Management | P1 | `EventTemplateManagementTest` covers stable keys, editable suggestions, copy-on-create isolation, Custom blank state, and warning-only dependencies. `EventLifecycleTest` covers direct CRUD, persisted modules, lifecycle, cancellation, duplication, correction, timezone conversion, and all-disabled completion. Event media remains planned. |
| M04 | Client Management | P1 | `ClientManagementTest`: create an individual or organization without a User, link Events/payments/communications, archive records with history, and flag duplicate contacts. `EventClientGateTest`: block leaving Draft without a Client. |
| M05 | Booking Management | P2 implemented in Prompt 14 | `OptionalBookingWorkflowTest`: create, review, approve/confirm in one manager step, reschedule/conflict override, cancel/finance flags, waitlist/promotion, idempotent conversion, and nullable Event links. The same suite and `EventLifecycleTest` prove the complete core workflow succeeds with a null Booking reference. |
| M06 | Venue Management | P2 implemented in Prompt 15 | `VenueManagementTest`: profile/branch/type, capacity, quote-first pricing architecture, facilities/rooms, derived availability, adjacent/overlapping and cancelled-event behavior, space isolation, transactional rejection, privileged audited override, optional-module preservation, and authorization. Protected-image behavior reuses `DocumentManagementTest`. Guest Management later extends capacity warnings with registered counts. |
| M07 | Vendor Management | P1 master implemented; P2 assignments and finance | `VendorManagementTest` covers no-login creation, categories, contacts, service areas, archive history, privileged unique account linking, authorization, and own-only portal visibility. `VendorAssignmentTest` later covers availability, work orders, contracts, invoices, payments, delivery, and ratings. |
| M08 | Staff Management | P1 master and P2 operations implemented | StaffManagementTest covers no-login master records and account linkage. StaffOperationsTest covers shifts, Event assignments, overlap/leave availability, authorized overrides, own schedule/status access, attendance inputs, performance storage, archive integrity, optional-module guards, and confidential simple salary/payment tracking without statutory payroll behavior. |
| M09 | Guest Management | P3 implemented in Prompt 23 | `GuestManagementTest`: no-login Guest CRUD/search/filter/archive structure, groups/families, VIP, plus-one/RSVP, Event-unique seating, private/operations notes, Venue capacity advisory, authorization, and module preservation. `GuestCheckInTest`: unique opaque tokens, PII-free QR, authorized Front Desk lookup, wrong-Event/revoked/invalid rejection, and idempotent duplicate scan with original time/operator. |
| M10 | Ticket Management | P3 | Implemented in Prompt 25. `TicketingManagementTest`: quantity-based VIP/Regular/Early Bird/custom types, sale/capacity availability, serialized issuance, promo validity/limits, immutable price snapshots, refunds and sold-out restoration, non-guessable local QR credentials, wrong-Event/status checks, duplicate scan detection, financial trace, RBAC, public publication, and disabled-module preservation. |
| M11 | Registration System | P3 | Implemented in Prompt 24. `RegistrationManagementTest`: required dynamic validation, option tampering, unpublished/disabled access, duplicate policy, authorization, approval/confirmation history, filtered CSV, optional idempotent Guest conversion, and Ticketing-disabled operation. |
| M12 | Task Management | P2 implemented in Prompt 18 | `TaskWorkflowTest`: create, filter, assign User/Staff, comment, securely attach, prioritize, complete with actor/time, derive overdue, isolate linked-Staff access, preserve status history, and guard disabled-module routes/data. |
| M13 | Budget Management | P2 implemented in Prompt 19; P4 reporting reconciliation | `EventBudgetTest` covers separate budget/actual storage, category variance, posted-only profit, linked reversals, permissions, Event isolation, and optional-module preservation. Prompt 21 confirms issuing an Invoice does not duplicate recognized Income; later reporting acceptance reconciles cross-module presentation. |
| M14 | Payment Management | P2 implemented in Prompt 20 and reconciled in Prompt 21; P4 reporting | `ManualPaymentLedgerTest` covers advance/partial/installment/final entries, schedules, allocations, due, receipts, unallocated overpayment, correction/refund history, finance integration, concurrency controls, permissions, optional-module preservation, and the absence of gateway routes. `InvoiceWorkflowTest` covers locked Invoice allocation limits and status/balance reopening after Refund. |
| M15 | Invoice System | P2 implemented in Prompt 21; P5 visual/output verification | `InvoiceWorkflowTest` covers lines, discount-before-tax rounding, prefix/year sequence, nullable Booking, immutable issue snapshots, cancellation/credit, partial/full/refunded balances, overpayment policy, PDF/email responses, search, authorization, and optional-module preservation. Production browser/PDF-printer/mail acceptance remains P5. |
| M16 | Inventory Management | P3; P4 reporting | `InventoryLedgerTest`: quantity-based stock movements reconcile and cannot be silently overwritten. `InventoryReservationTest`: active Event reservations affect availability, damage is traceable, and low stock creates a notification. |
| M17 | Catering Management | P3 | `CateringPlanTest`: menu, package, quantity, schedule, dietary data, vendor link, and cost. Authorization test restricts dietary data; finance integration test posts approved costs once. |
| M18 | Decoration Management | P3 | `DecorationPlanTest`: Event-specific theme/elements, reference files, revisions/notes, Vendor and Inventory links. Finance integration test prevents duplicate cost totals. |
| M19 | Transportation Management | P3 | `TransportationPlanTest`: vehicle, driver, route, pickup, guest/group, time, and status. `TransportConflictTest`: overlapping vehicle/driver assignments are flagged and costs reach Event expenses. |
| M20 | Accommodation Management | P3 | `AccommodationAllocationTest`: booking, room allocation, stay dates, check-in/out, and charges. Capacity test blocks over-allocation unless an authorized override exists. |
| M21 | Marketing Module | P3 | `MarketingPlanningTest`: campaigns, audience, content/link tracking, coupons, referrals, consent, opt-out, and metrics. `NoSocialPublishingTest`: no direct social API operation is exposed. |
| M22 | Communication Center | P2 implemented; P5 production-provider verification | `CommunicationCenterTest`: templates, recipients, Event/Client/Booking context, consent category, status, immutable attempts, synchronous send, disabled-provider behavior, sanitized failure, authorized retry, and optional-module preservation. |
| M23 | Calendar | P2 implemented in Prompt 18 | `CalendarProjectionTest`: daily/weekly/monthly entries derive from Events and enabled Venue/Staff/Vendor/Task schedules without a source table; date-window, ownership, disabled-source, and authorized click-through assertions cover visibility. |
| M24 | Document Management | P1; P5 security hardening | `ProtectedDocumentTest`: allowed upload types/sizes, private storage, contextual links, authorized download, and denial of direct unauthorized access. Expiry test creates the configured alert. |
| M25 | Reports | P4; P5 export/browser verification | `ReportReconciliationTest`: Event, finance, Client, Vendor, Staff, and Inventory totals match source records. Filter/export tests verify PDF/CSV/Excel output and permission controls. |
| M26 | Analytics Dashboard | P4; P5 performance verification | `AnalyticsMetricTest`: documented definitions, period comparison, growth, popular types, performance metrics, and financial reconciliation. Query-count and response-time tests cover agreed data volume. |
| M27 | Notifications | P2 implemented; P5 cron/operations verification | `CommunicationCenterTest`: correct isolated recipient, role materialization, read/unread persistence, safe source link, escaped output, permission scope, and idempotent command execution by request/refresh with no browser push, WebSocket, Redis, or queue worker. |
| M28 | Settings | P1; P5 deployment verification | `SettingsTest`: company, disabled-by-default branches, timezone, currency, language, tax, invoice prefix, integrations, security, and backup metadata. Secret encryption/masking and audit tests cover sensitive changes. |
| M29 | Audit and Security | P1; P5 hardening | `AuditTrailTest`: sensitive actions append actor/action/entity/time/before-after metadata and normal users cannot alter it. `LoginHistoryTest`: success/failure, IP, time, and user agent are filterable. Backup/restore evidence is covered in NFR-06. |

## Cross-module functional requirement traceability

| Requirement | Planned phase | Planned test evidence |
| --- | --- | --- |
| FR-X01 Global search | P1 framework; Booking and Invoice adapters implemented | `DashboardAndSearchTest` covers Events, Clients, Vendors, and Users; `OptionalBookingWorkflowTest` covers Booking; `InvoiceWorkflowTest` covers permission-aware, Event-scoped, bounded Invoice search without hidden disabled-module records. |
| FR-X02 Lists and export | P1 shared components; all later phases | Contract tests require server-side pagination, stable sorting, filters, bounded page size, and filter-consistent export on every applicable list. |
| FR-X03 Non-destructive history | P1 pattern; all phases | `ArchivalIntegrityTest` verifies archive/inactive/soft-delete behavior and retained financial, relationship, and audit references. |
| FR-X04 Status history | P1 pattern; Booking implemented | Shared status-transition tests and `OptionalBookingWorkflowTest` require actor, timestamp, reason, valid transitions, and append-only change evidence. |
| FR-X05 Secured file service | P1; P5 | `DocumentAuthorizationTest` covers reusable contextual attachments, private paths, signed/controller-mediated access, and direct-link denial. |
| FR-X06 Organization formatting | P1; P5 | `OrganizationFormattingTest` covers configured timezone, currency, language, invoice rendering, and date/money consistency. |
| FR-X07 Optional branch scope | P1 schema; P5 enablement test | `BranchDisabledDefaultTest` proves single-company records work without branch selection. `BranchScopeTest` is prepared for later enablement and checks assigned-branch isolation. |
| FR-X08 Configurable master data | P1; P2-P3 consumers | CRUD and authorization tests cover Event, Vendor, Inventory, Finance, and Document categories plus Staff Departments without code changes. |
| FR-X09 Event without Booking | P1 implemented; finance regressions implemented | `EventLifecycleTest` and `OptionalBookingWorkflowTest` run direct Client-to-Event creation through completion with null booking_id and all optional modules disabled. `ManualPaymentLedgerTest` and `InvoiceWorkflowTest` prove Payment and Invoice workflows also retain nullable Booking. |
| FR-X10 Editable Event modules | P1 | `EventTemplateManagementTest` verifies suggested defaults, manager overrides, later template isolation, editable templates, unknown-key rejection, and no hard-coded template lock. `EventLifecycleTest` verifies transactional Event-owned initialization; `EventWorkspaceTest` verifies manager-only changes, origin presentation, warnings, and immutable change history. |
| FR-X11 Domain records without login | P1 | `NullablePortalIdentityTest` creates and uses Client, Vendor, and Staff records with null `user_id`; later account linking is separately authorized. |
| FR-X12 Preserve disabled-module data | P1 pattern; P2-P3 modules | `EventWorkspaceTest` requires general and data-specific confirmation plus reason, hides normal navigation, denies/redirects direct routes, retains a protected Document link, records actor/time/history, and restores access after re-enable. Every later module must add enabled/disabled tests using its registered detector. |
| FR-X13 Disabled modules impose no fields | P1 pattern; P2-P3 modules | `EventLifecycleTest` completes a small Event while every non-core module is disabled; `EventWorkspaceTest` proves disabled workspaces are absent/inaccessible without adding core-form validation. |

## Non-functional requirement traceability

| Requirement | Planned phase | Planned test evidence |
| --- | --- | --- |
| NFR-01 Security | P1 controls; P5 hardening | Feature tests for authentication, policies, CSRF, validation/escaping, mass assignment, rate limits, secure hashing, HTTPS production configuration, and common authorization bypasses. |
| NFR-02 Privacy | P1 data classification; P5 review | Permission tests for personal, payment, and dietary data; retention/export/deletion checks after policy values are confirmed; log and error-message inspection for sensitive data. |
| NFR-03 Performance | P5 | Repeatable baseline dataset and timed tests for typical authenticated list/detail actions, targeting under 2 seconds excluding third-party latency; record query counts and host profile. |
| NFR-04 Scalability | P1 query conventions; P5 load tests | Pagination limits, index review using query plans, bounded eager loading, synchronous workload tests, and an architecture assertion that Redis/Horizon/persistent workers are not required. |
| NFR-05 Availability | P5 | Health endpoint, production error handling, Laravel log checks, cPanel/Apache error-log runbook, and maintenance/recovery exercise after targets are agreed. |
| NFR-06 Backup | P5 | Scheduled command smoke test, protected database/file backup artifact, retention test after policy confirmation, restore into an isolated database, checksum/record validation, and documented recovery result. |
| NFR-07 Auditability | P1; P5 | Coverage matrix for refunds, cancellations/deletes, settings, permissions, restore, login/security events, and privileged corrections; immutability and retention tests. |
| NFR-08 Accessibility | P1 component rules; P5 audit | Keyboard-only walkthrough of core Client to Event flow, semantic label checks, focus visibility, contrast scan, error association, and responsive zoom/manual review. |
| NFR-09 Compatibility | P5 | Browser matrix for latest two major Chrome, Edge, Firefox, and Safari versions at desktop, tablet, and mobile breakpoints; PDF/print verification on agreed targets. |
| NFR-10 Localization | P1 architecture; P5 verification | Locale/timezone/currency configuration tests, translation-key coverage, UTF-8 data round-trip, and no hard-coded currency/date output in business screens. |
| NFR-11 Observability | P2 integrations; P5 operations | Tests and manual log review for application exceptions, provider failures, scheduled command failures, and payment-recording errors; secrets and personal data must be redacted. |
| NFR-12 Data integrity | P1 patterns; Booking, Finance, Invoice, and Ticketing implemented | Booking conversion and financial/ticket operations use row locks, transactions, restrictive foreign keys, immutable snapshots, unique idempotency/credential constraints, append-only history, and focused integrity tests. Stock integrity remains with Inventory. |
| NFR-13 Shared hosting compatibility | P0; P5 | Production-like deployment test with prebuilt assets, writable `storage` and `bootstrap/cache`, MySQL/MariaDB, HTTPS, scheduler via cron when available, and no Node runtime, Redis, WebSocket server, or long-running worker. |

## Traceability maintenance rules

- A test name is a planned identifier until the test file exists; implementation may refine the class name without weakening coverage.
- Each change to an SRS requirement must update this matrix, `DECISIONS.md`, affected tests, and `ARCHITECTURE.md` or `DATABASE_ROADMAP.md` when it changes a boundary or schema contract.
- Optional Event modules require tests in both enabled and disabled states.
- A passing UI test never substitutes for server-side authorization, transaction, or constraint tests.
- Phase completion requires all mapped Must requirements for that phase, no open critical/high-severity defects, and recorded user acceptance evidence.
